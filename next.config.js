/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Add your image domains here when you have real images
    remotePatterns: [],
  },
};

module.exports = nextConfig;
